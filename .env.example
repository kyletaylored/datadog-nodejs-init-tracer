# Datadog org/API (for the Agent only)
DD_API_KEY=your_api_key_here
DD_SITE=datadoghq.com

# App metadata
DD_SERVICE=datadog-nodejs-init-tracer
DD_ENV=dev
DD_VERSION=1.0.0

# RUM (optional; UI shows Enabled only if set)
DD_APPLICATION_ID=
DD_CLIENT_TOKEN=

# Web port (container exposes 3000)
PORT=3000